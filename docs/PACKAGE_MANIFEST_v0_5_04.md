# Package manifest — InventorIptOrg v0.5.04

Build name: `OPAQUE_BODY_MESH`

Archive name: `InventorIptOrg_v0_5_04_OpaqueBodyMesh.zip`

Main changes:

- new default mode `Mesh Opaque Body — v0.5.04`;
- explicit opaque surface alpha `255`;
- per-pixel front-surface triangle-owner buffer;
- quantized 3D edge-to-surface-owner mapping;
- owner-match gate for ordinary visible POLY_LOOP mesh samples;
- owner-match counters in selection/render logs;
- v0.5.03 visual palette and 65% ratio gate preserved.

Stable components retained: software Z-buffer, hidden-line removal, Gouraud shading, visible POLY_LOOP mesh, no fan-triangle diagonals, AdvancedFace orientation, historical Mode versions and FastDragWireframe.

No Visual Studio or Autodesk Inventor run is claimed for package creation.
