# Package manifest — InventorIptOrg v0.5.02

Build name: `RENDER_METHOD_LAB_LOGGING`

Archive name: `InventorIptOrg_v0_5_02_RenderMethodLabLogging.zip`

Main change: multiple Surface+Mesh shading methods are selectable in the existing Mode combo, and every selection plus every completed Z-buffer render records the exact render-method profile in the application log.

Stable renderer components retained: software Z-buffer, hidden-line removal, visible POLY_LOOP mesh, no fan-triangle diagonals, AdvancedFace orientation, Gouraud interpolation, FastDragWireframe.

No Visual Studio or Autodesk Inventor run is claimed for package creation.
