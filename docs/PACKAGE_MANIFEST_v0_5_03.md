# Package manifest — InventorIptOrg v0.5.03

Build name: `MESH_VISIBILITY_THRESHOLD`

Archive name: `InventorIptOrg_v0_5_03_MeshVisibilityThreshold.zip`

Main changes:

- historical method-version suffix on every Mode item;
- `AppVersion` and `MethodVersion` in selection and render logs;
- 3×3 front-depth neighborhood for `Mesh Inventor Lab — v0.5.03`;
- 65% visible-sample ratio gate for ordinary POLY_LOOP mesh edges;
- accepted/rejected mesh-edge counters.

Stable components retained: software Z-buffer, hidden-line removal, Gouraud shading, visible POLY_LOOP mesh, no fan-triangle diagonals, AdvancedFace orientation and FastDragWireframe.

No Visual Studio or Autodesk Inventor run is claimed for package creation.
