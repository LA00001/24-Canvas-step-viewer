# Next task after v0.4.31

v0.4.31 реанимирует Create feature browser folder.

Дальше можно тестировать:
- маленький выбор через `.ipt cubes`;
- создание feature folder из текущего visible-list;
- поведение `Copy features to clipboard`;
- нужно ли добавлять отдельную кнопку `Build features from current bodies`.

`Apply edited name / XYZ to Inventor` пока отложено.
